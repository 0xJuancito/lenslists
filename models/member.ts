/**
 * @swagger
 * components:
 *   schemas:
 *     Member:
 *       type: object
 *       properties:
 *         profileId:
 *           type: string
 */
export interface Member {
  profileId: string;
}
