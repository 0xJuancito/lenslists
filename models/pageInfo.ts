/**
 * @swagger
 * components:
 *   schemas:
 *     PageInfo:
 *       type: object
 *       properties:
 *         totalCount:
 *           type: number
 */
export interface PageInfo {
  totalCount: number;
}
